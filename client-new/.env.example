# API URLs are not read from .env in this app.
# Edit: public/app-config.json
#   apiUrlDevelopment  → used with ng serve / dev build (isDevMode)
#   apiUrlProduction   → used with production build
# Use "" for same-origin requests (e.g. dev proxy to /api).
